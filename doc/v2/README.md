# v2 Documents

`doc/v2/` は次の 2 系統に分かれています。

- `core/`: 継続参照する仕様・コンセプト
- `log/`: 作業時点の計画、レビュー、進捗、差分記録

運用ルール:

- 現在の仕様判断や設計判断の基準は `core/` を優先する
- 日付付きの文書、レビュー、スナップショット、差分比較は `log/` に置く
- `log/` の文書は作成日時点の記録であり、現状と差分があっても履歴として保持する
